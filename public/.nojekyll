# Tells GitHub Pages to skip Jekyll, which silently drops every path starting
# with an underscore — i.e. all of Next's output under _next/.
# Symptom if missing: HTML loads, every asset 404s, page renders unstyled, no error anywhere.
